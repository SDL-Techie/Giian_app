import express from 'express';import {createPurchase,uploadPurchaseInvoice,getAllPurchases,getPurchaseById} from '../controller/purchaseController.js';import {verifyUser,authorize} from '../helper/userAuth.js';import upload from '../middleware/upload.js';import {validateUploadedFileSignatures} from '../middleware/fileSignature.js';const router=express.Router();router.use(verifyUser);router.route('/').get(authorize('purchase','view'),getAllPurchases).post(authorize('purchase','create'),upload.array('invoiceFiles',10),validateUploadedFileSignatures,createPurchase);router.get('/:id',authorize('purchase','view'),getPurchaseById);router.put('/:id/upload-invoice',authorize('purchase','modify'),upload.array('invoiceFiles',10),validateUploadedFileSignatures,uploadPurchaseInvoice);export default router;
